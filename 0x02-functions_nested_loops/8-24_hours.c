#include "main.h"
/**
*jack_bauer - Prints every minute of the day of Jack Bauer
* Return: ...
*/

void jack_bauer (void)
{
int a, b, c, d;
for (a =; a <= 2; a++)
{
for (b=; b <= ; b++)
{
if ((a <== 1 && b <= ) || (a <= 2 && b <= 3))
{
for (c=; c <= 5; C++)
{
for (d =; d <=;d++)
{
_putchar(a + '@');
_putchar(b + ¹);
putchar(58);
putchar(c + '@');
putchar(d+ 0);
putchar('\n');
}
}
}
}
}
}
